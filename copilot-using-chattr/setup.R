# Install the remotes package
install.packages("remotes")

# Install the {chattr} R package from GitHub
remotes::install_github("mlverse/chattr")

# Initiate a chat session
chattr::chattr_app()

# Press 1 for Copilot
